<h1>Listing weeks</h1>

<table>
  <tr>
    <th>User</th>
    <th>Start</th>
  </tr>

<% @weeks.each do |week| %>
  <tr>
    <td><%=h week.user_id %></td>
    <td><%=h week.start %></td>
    <td><%= link_to 'Show', week %></td>
    <td><%= link_to 'Edit', edit_week_path(week) %></td>
    <td><%= link_to 'Destroy', week, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New week', new_week_path %>